name: Scrape & Deploy

on:
  schedule:
    - cron: '0 5 * * 1'   # Mondays 5am UTC = 8am Helsinki (UTC+3)
  workflow_dispatch:           # manual trigger from GitHub UI

concurrency:
  group: gh-pages-deploy
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright + WebKit
        run: python -m playwright install --with-deps webkit

      - name: Restore caches
        uses: actions/cache/restore@v4
        with:
          path: data/
          key: never-match
          restore-keys: oikotie-cache-

      - name: Run scraper
        run: python retry_scraper.py
        env:
          DATA_DIR: data

      - name: Save updated caches
        uses: actions/cache/save@v4
        with:
          path: data/
          key: oikotie-cache-${{ github.run_id }}

      - name: Copy static PWA assets
        run: |
          cp -r static/. data/
          grep -q '__SW_CACHE_VERSION__' data/sw.js || { echo "::error::placeholder __SW_CACHE_VERSION__ missing from sw.js — update was not applied"; exit 1; }
          sed -i "s/__SW_CACHE_VERSION__/$(git rev-parse --short HEAD)/g" data/sw.js

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./data
          publish_branch: gh-pages
          include_nojekyll: true
